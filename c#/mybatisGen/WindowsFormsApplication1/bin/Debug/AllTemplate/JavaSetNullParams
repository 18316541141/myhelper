package web.template.setNullParams.codeGenerator;
import java.util.Date;
/**
 * $entity.EntityNotes$
 */
public class $entity.EntityName$SetNullParams {
$entity.PropList:{
	$if(it.ParamsTypeIsEqual)$
		$if(it.IsKey)$
		/**
		 * $it.PropNotes$
		 */
		private $it.PropType$ $it.PropName$;

		public void set$it.CapUpperPropName$($it.PropType$ $it.PropName$){
			this.$it.PropName$=$it.PropName$;
		}

		public $it.PropType$ get$it.CapUpperPropName$(){
			return this.$it.PropName$;
		}
		$else$
		/**
		 * $it.PropNotes$
		 */
		private boolean $it.PropName$;

		public void set$it.CapUpperPropName$(boolean $it.PropName$){
			this.$it.PropName$=$it.PropName$;
		}

		public boolean get$it.CapUpperPropName$(){
			return this.$it.PropName$;
		}
		$endif$
	$endif$
}$
}